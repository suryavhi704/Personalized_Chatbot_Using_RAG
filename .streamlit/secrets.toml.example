# Copy this file to .streamlit/secrets.toml for LOCAL development
# (that copy is gitignored and must never be committed).
#
# On Streamlit Cloud, don't upload this file at all — instead paste
# the same key into: App settings -> Secrets, in the same TOML format.

GROQ_API_KEY = "your-groq-api-key-here"
